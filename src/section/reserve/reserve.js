const box = document.querySelector('.reserve .box');
const front = box.querySelector('.front');
const end = box.querySelector('.end');

window.addEventListener("DOMContentLoaded", () => {
    front.style.transition = 'transform 0.7s';
    end.style.transition = 'transform 0.7s';
});


